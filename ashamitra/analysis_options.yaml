include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: false
  errors:
    invalid_annotation_target: ignore
    # Suppress false-positive from flutter analyze CLI on this SDK version
    # The code compiles correctly — confirmed via `flutter build web`
    undefined_annotation: ignore
